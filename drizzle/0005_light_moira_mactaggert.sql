ALTER TABLE "campaigns" ADD CONSTRAINT "campaigns_review_states_recommendations_check" CHECK (case when "campaigns"."state" in ('niche_review', 'discovery_ready') then case when jsonb_typeof("campaigns"."niche_recommendation_ids") = 'array' then jsonb_array_length("campaigns"."niche_recommendation_ids") > 0 else false end else true end);--> statement-breakpoint
ALTER TABLE "campaigns" ADD CONSTRAINT "campaigns_discovery_ready_approved_check" CHECK (case when "campaigns"."state" = 'discovery_ready' then case when jsonb_typeof("campaigns"."approved_niche_ids") = 'array' then jsonb_array_length("campaigns"."approved_niche_ids") > 0 else false end else true end);